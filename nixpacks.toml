[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --no-audit --no-fund"]

[phases.build]
cmds = [
  "npx prisma generate",
  # NOTE: prisma db push is better done via Pre-deploy command in Railway or in Dockerfile
  "npm run build"
]

[start]
cmd = "node server.js"