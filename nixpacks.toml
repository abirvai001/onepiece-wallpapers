[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npx prisma db push",
  "npm run build",
]

[start]
cmd = "npm start"