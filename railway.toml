[build]
builder = "NIXPACKS"
buildCommand = "npm ci --omit=dev && npx prisma generate && npx prisma db push && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3