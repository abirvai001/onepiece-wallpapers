[build]
# When Dockerfile is present, Railway will use Docker build (recommended for reliable standalone)
# buildCommand below is only used if no Dockerfile or nixpacks forced.

[deploy]
# Matches the CMD in Dockerfile. Falls back gracefully if using nixpacks.
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3